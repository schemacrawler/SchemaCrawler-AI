spring:
  main:
    banner-mode: off
  application:
    name: schemacrawler-mcpserver
server:
  name: "SchemaCrawler MCP Server"
  heartbeat: true
ai:
  mcp:
    server:
      name: schemacrawler-mcpserver
      capabilities:
        completion: false
        prompt: false
management:
  endpoints:
    web:
      exposure:
        include: info
