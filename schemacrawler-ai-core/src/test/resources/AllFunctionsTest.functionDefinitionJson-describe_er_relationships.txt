{
  "name" : "describe_er_relationships",
  "title" : "Describe relationships in the ER model; use describe_tables for physical schema details",
  "description" : "Generates detailed documentation for relationships in the ER model, including 1..1, 1..M, M..N and optional relationships. Supports regex-based relationship name filtering  to optimize tool performance. Returns conceptual ER-model relationships as a JSON object. For raw physical schema details such as columns, constraints, and DDL, use describe_tables.",
  "inputSchema" : {
    "type" : "object",
    "properties" : {
      "relationship_name" : {
        "type" : "string",
        "description" : "Name of relationships to describe, from the ER model.\nMay be specified as a regular expression, matching the fully qualified\nrelationship name (including the schema).\nTry not to match all relationships, but instead use a regular expression\nto match a subset or match a single relationships, since otherwise results may\nbe large.\n"
      },
      "cardinality" : {
        "type" : "string",
        "description" : "Indicates the types of relationships to return - for example, 1..1, 1..M, M..N\nand optional relationships.\n",
        "enum" : [ "ALL", "ZERO_ONE", "ZERO_MANY", "ONE_ONE", "ONE_MANY", "MANY_MANY" ]
      }
    },
    "required" : [ ],
    "additionalProperties" : false
  }
}
