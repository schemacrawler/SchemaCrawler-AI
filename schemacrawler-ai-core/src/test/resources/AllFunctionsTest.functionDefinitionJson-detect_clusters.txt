{
  "name" : "detect_clusters",
  "title" : "Detect table clusters",
  "description" : "Detects table clusters - also known as communities, cliques, modules, or subsystems - of tightly related tables and views, based on how they are connected by foreign keys and other relationships. Use this tool to discover system domains, bounded contexts, or functional areas within a database schema, without having to know table names in advance. Table clusters can be filtered by a regular expression matching any member's fully qualified name. Each table cluster includes the full names of its member tables and views, and the anchor table (its most important, or representative, member).",
  "inputSchema" : {
    "type" : "object",
    "properties" : {
      "table_name" : {
        "type" : "string",
        "description" : "Name of a database table or view used to select table clusters. A table cluster\nis returned when any member matches.\nMay be specified as a regular expression, matching the fully qualified\ntable name (including the schema).\nTry not to match all tables, but instead use a regular expression\nto match a subset or match a single table, since otherwise results may\nbe large.\n"
      },
      "max_clusters" : {
        "type" : "integer",
        "description" : "Maximum number of table clusters to return. Defaults to 5.\n-1 returns all matching table clusters without limiting.\n"
      },
      "max_cluster_size" : {
        "type" : "integer",
        "description" : "Maximum number of member tables and views to include per table cluster.\nDefaults to 5.\n-1 returns all members without limiting.\n"
      }
    },
    "required" : [ ],
    "additionalProperties" : false
  }
}
