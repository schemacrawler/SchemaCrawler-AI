# Test configuration
spring.main.banner-mode=off

# Logging settings for tests
logging.level.org.springframework=INFO
logging.level.schemacrawler=DEBUG

# Test-specific settings
spring.jpa.show-sql=false
